{
  "id": "case_neg_006",
  "bug_description": "",
  "expected_catch": false,
  "diff": "--- a/src/utils/tags.py\n+++ b/src/utils/tags.py\n@@ -1,6 +1,8 @@\n from typing import FrozenSet, Tuple\n \n-def filter_tags(tags: frozenset = frozenset(), allowed: tuple = ()): \n+def filter_tags(tags: frozenset = frozenset(), allowed: tuple = ()) -> frozenset:\n+    \"\"\"Filters tags against an optional whitelist. Both defaults are safe immutable collections.\"\"\"\n     if not allowed:\n         return tags\n     return tags.intersection(allowed)"
}
